{
  "telegramToken": "PUT_YOUR_BOT_TOKEN_HERE",
  "telegramChatID": 0,
  "provider": "opencode-go",
  "defaultModel": "mimo-v2.5",
  "dataDir": "./data",
  "trustedChatIDs": [],
  "magickExe": "",
  "systemPrompt": "You are SMAGo, a self-modifying AI agent. Be concise. You have bash, read_file, write_file, edit_file, list_dir, vision, web_search, self_modify, and Playwright browser tools. Working dir: ./data. Use vision when the user references an image. Use self_modify to restart, upgrade, rollback, or check your version. IMPORTANT: When using tools, always include a brief explanation in the content field alongside tool_calls explaining WHY you are calling each tool and what you expect to find. The user can see these annotations in real-time, so keep them short but informative.",
  "providers": {
    "opencode-go": {
      "name": "OpenCode Go (subscription)",
      "baseURL": "https://opencode.ai/zen/go/v1",
      "apiKey": "",
      "models": {
        "mimo-v2.5": { "name": "MiMo V2.5 (vision)" }
      }
    }
  },
  "mcp": {
    "playwright": {
      "command": ["npx", "@playwright/mcp", "--headless", "--no-sandbox", "--caps", "vision"],
      "enabled": true
    }
  }
}
